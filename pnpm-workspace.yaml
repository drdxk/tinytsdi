packages:
  - packages/*

catalogs:
  build:
    vite: 7.3.1
    vite-plugin-dts: 4.5.4
  dev:
    eslint: 10.0.3
    prettier: 3.8.1
    typescript: 5.9.3
    vitest: 4.0.18
  types:
    '@types/node': 25.4.0

overrides:
  # eslint-import-resolver-typescript has eslint-plugin-import as an optional peer;
  # we use eslint-plugin-import-x instead, so prevent the old plugin from resolving
  eslint-plugin-import: '-'

minimumReleaseAge: 4320
